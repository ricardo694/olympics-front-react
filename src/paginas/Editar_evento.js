import React from "react";
import Formu_editar_evento from "../componentes/Formu_editar_evento";

export default class Editar_evento extends React.Component{

    state={

    }

    render(){
        return(
            <div>
                <Formu_editar_evento/>
            </div>
        )
    }

}